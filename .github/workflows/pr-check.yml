name: PR Quality Check

on:
  pull_request:
    branches:
      - main
      - dev

permissions:
  contents: read

concurrency:
  group: pr-quality-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  quality:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    env:
      VITE_WHISPER_API_URL: http://localhost:5001
      VITE_BACKEND_API_URL: http://localhost:5000

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22.11.0
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      # TODO: 기존 Settings 테스트의 axios mock을 별도 PR에서 수정한 뒤 제거한다.
      - name: Test (advisory)
        continue-on-error: true
        run: npm test -- --run

      - name: Build
        run: npm run build

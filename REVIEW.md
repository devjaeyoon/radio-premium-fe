# RadioPremium FE 리뷰 가이드

이 문서는 코드 작성자와 리뷰 도구가 같은 기준으로 변경 사항을 검토하기 위한
저장소의 리뷰 기준이다. 형식보다 실제 동작의 정확성과 회귀 방지를 우선한다.

## 리뷰 우선순위

다음 순서로 문제를 판단한다.

1. 보안, 개인정보 노출, 데이터 손실 가능성
2. 핵심 기능의 오동작과 사용자·재생 세션 불일치
3. 비동기 자원의 수명주기, 경쟁 상태, 오류 누락
4. FE-BE-AI HTTP 및 socket 계약의 호환성
5. 회귀 테스트, 접근성, 유지보수성

자동 포매터가 고칠 수 있는 형식만으로 변경을 차단하지 않는다. 현재 PR에서 실제로
발생할 수 있는 문제와 재현 조건을 우선 설명하고, 범위를 벗어난 대규모 재설계는
후속 Issue로 제안한다.

## 핵심 불변 조건

### 사용자 세션

- 사용자 ID의 생성과 복구는 한 곳에서 소유한다.
- ID가 아직 없거나 잘못된 경우 숫자 `0`을 사용자 ID로 사용하지 않는다.
- 모듈 로드 시점의 localStorage 값을 장기간 재사용하지 않는다.
- Strict Mode와 여러 컴포넌트가 동시에 mount되어도 사용자 생성은 한 번만 수행한다.
- 사용자가 바뀌면 이전 사용자의 설정, 즐겨찾기, 임시 상태를 상속하지 않는다.

### 재생 세션

- 실제 `play()` 성공 전에는 UI를 재생 중으로 표시하지 않는다.
- HLS, media element, listener와 timer의 소유자 및 cleanup 시점을 명확히 한다.
- pause, close, channel switch, unmount 이후 도착한 callback은 무시한다.
- 채널 전환은 성공 후 상태를 확정하며, 실패하면 기존 재생 상태를 보존한다.
- socket event는 가능하면 session, job, channel, sequence와 연결하여 오래되거나 중복된
  이벤트가 현재 세션을 바꾸지 못하게 한다.

### API와 오류

- 비동기 실패를 `undefined`, 빈 값 또는 성공 응답으로 변환하지 않는다.
- 호출자가 실패를 처리할 수 있도록 오류를 반환하거나 throw하고 UI 상태에 연결한다.
- Whisper 작업의 시작과 중지는 동일한 서버 경계와 명시적인 payload 계약을 사용한다.
- 설정 화면의 값과 실제 실행 중인 Whisper 작업의 동작이 일치해야 한다.
- 광고 제보가 실패하면 modal을 성공처럼 닫거나 채널을 이동하지 않는다.
- 환경변수, 인증 정보, 사용자 데이터와 전사 내용은 로그에 노출하지 않는다.

### 상태와 mutation

- 서버 데이터와 재생 중에만 유효한 임시 UI 상태를 같은 방식으로 영속화하지 않는다.
- 즐겨찾기, 순서 변경, 설정, 제보 mutation에는 pending, error와 실패 복구 정책이 있다.
- 파생 가능한 값은 별도 state와 effect로 복제하지 않는다.

## 테스트 기준

- 버그 수정은 수정 전 실패하고 수정 후 통과하는 회귀 테스트를 포함한다.
- 성공 경로 외에 API 실패, 중복 요청, 역순 callback, cleanup과 rollback을 검증한다.
- mock 호출 여부만 확인하지 말고 사용자가 관찰하는 결과와 자원 상호작용을 검증한다.
- 변경하지 않은 핵심 동작도 영향을 받는다면 관련 회귀 테스트를 함께 실행한다.
- 테스트를 추가할 수 없으면 PR에 이유와 수동 검증 절차를 기록한다.

## 코드 규칙

- `.prettierrc`와 `eslint.config.js`를 형식 및 정적 분석의 기준으로 사용한다.
- 문자열은 큰따옴표, 들여쓰기는 2칸, 한 줄은 80자를 기준으로 한다.
- 한 개의 매개변수도 화살표 함수의 괄호를 생략하지 않는다.
- 내부 모듈은 `@/` 절대 경로를 우선하고 import 경로의 확장자는 생략한다.
- 컴포넌트와 페이지는 PascalCase, boolean은 `is` 또는 `has`, 함수는 동사로 시작한다.
- 하나의 컴포넌트를 제공하는 파일은 default export, 여러 유틸 함수를 제공하는 파일은
  named export를 사용한다.

## PR 완료 조건

- 연결된 Issue의 요구사항과 완료 조건을 충족한다.
- `npm run lint`, `npm test -- --run`, `npm run build`가 통과한다.
- API 또는 socket 계약 변경 여부와 다른 저장소에 미치는 영향을 PR에 기록한다.
- 알려진 위험, 미검증 항목, rollback 방법을 PR에 기록한다.
- 유효한 리뷰 의견을 반영하고 최신 커밋을 다시 검증한다.
